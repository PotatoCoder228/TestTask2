# The testing task 2

## How to build

With make:
```bash
make
```

## Tests

For results validation I manually prepared the testing data in `data/`.
Шt doesn't completely match the output of the program - these are just manual examples to understand what result I want to get. 
`output.html` - this is an example of the output generated by the program.